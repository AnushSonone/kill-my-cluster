# Copy to .env beside this file. Never commit .env.
#
# Grafana admin password. The dashboards are provisioned from files and the
# public embed uses anonymous Viewer, so this only guards the admin account.
# Generate one with:  openssl rand -base64 24
GF_ADMIN_PASSWORD=replace-me
